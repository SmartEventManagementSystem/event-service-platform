# Services
apiVersion: v1
kind: Service
metadata:
  name: event-service
  namespace: default
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: event-service
